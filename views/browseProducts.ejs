<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Document</title>
</head>

<body>
    <div class="container">
        <h1 class="text-center">Welcome <%= cust.name %> with <%= cust.email %> !</h1>
        <h3 class="text-center">Here are the products you can select from.</h3>

        <ul>
            <% for(var i=0; i<store_list.length; i++){ %>
                <li>
                    <h4>
                        <%= store_list[i].store %>
                    </h4><br>
                    <% var prod_arr=JSON.stringify(store_list[i].products) %>
                        <% prod_arr=JSON.parse(prod_arr) %>
                            <% var item=JSON.stringify(prod_arr) %>
                                <% item=JSON.parse(item) %>
                                    <% for( var j=0; j<item.length; j++) { %>
                                        <%= item[j].item_name %> <br>
                                            <%= item[j].description %> <br><br>
                                                <% } %>
                </li>
                <% } %>
        </ul>
    </div>


</body>

</html>